{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creation de voiture</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider"  class="btn btn-success"/>
    {{ form_end(form) }}


            <a href="{{ path('voiture_index') }}" class="btn btn-default">Back to the list</a>

{% endblock %}
